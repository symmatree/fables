[[nuc-g3p-1 - Proxmox Virtual Environment]]
Compute cluster hosting some (all?) of the nodes that serve in the [[Tiles (cluster)]] . Possibly also a Ceph instance, instead of minio inside the cluster. Maybe just use the NAS for shared storage and leave local for ephemeral?

[[ProxMoxNodeSetup]] is how I built them
[[proxmox-data-collection]] pulls current data